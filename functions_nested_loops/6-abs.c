#include "main.h"
/**
* _abs - Entry point
* @r: The number to be collected
* Return: Always 0 (Success)
*/
int _abs(int r)
{
	if (r > 0)
	{
		return (r);
	}
	else
	{
		return (r * -1);
	}
return (0);
}
