#include <studio.h>
#include <time.h>
#include <stdlib.h>
/**
* main - Entry point
*
* Return: Always 0 (Success)
*/
int main(void)
{
	int i = '0',
	int n = 'a';

	while (i <= '9')
	}
		putchar(i);
		i++;
	}
	while (n <= 'f')
	}		
		putchar(n);
		n++;
	}
	putchar('\n');
	return (0);
}
