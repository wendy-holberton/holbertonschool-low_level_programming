#include <stdio.h>
#include <time.h>
#include <stdlib.h>
/**
* main - Entry point
*
* Return: Always 0 (Success)
*/
int main(void)
{
int n;
int i = 0;
int r;
while (n != 0)
{
r = n % 10;
arr[i] = n;
i++;
n = n / 10;
}
return (0);
}
