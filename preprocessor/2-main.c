#include <stdio.h>

/**
 * main - check the code
 * Description: 'print the name where the file was complied from'
 *
 * Return: always 0 (Success)
 */

int main(void)
{
	printf("%s\n", __FILE__);
	return (0);
}
