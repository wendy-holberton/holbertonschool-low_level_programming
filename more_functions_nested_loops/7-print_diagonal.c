#include "main.h"
/**
* print_diagonal - Entry point
* @n: The number of times

* Return: Always 0 (Success)
*/
void print_diagonal(int n)
{
	int a;

	for (a = 48; a <= 57; a++)
	{
		if (a != 50 && a != 52)
		_putchar(a);
	}
	_putchar('\n');
}
